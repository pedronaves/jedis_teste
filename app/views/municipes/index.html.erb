<p style="color: green"><%= notice %></p>

<h1>Municipes</h1>

<div id="municipes">
  <% @municipes.each do |municipe| %>
    <%= render municipe %>
    <p>
      <%= link_to "Ver", municipe %>
    </p>
  <% end %>
</div>

<%= link_to "Criar Novo", new_municipe_path %>
